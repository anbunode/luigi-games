# Shim de compatibilidad Render

Si tu servicio en Render tiene **Root Directory = `backend`**, este folder reenvía el build al motor real en `../skrepayshop-api/`.

**Recomendado:** cambia en Render → Settings → Root Directory → `skrepayshop-api`.
