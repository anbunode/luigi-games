services:
  - type: web
    name: skrepayshop-api
    runtime: node
    plan: starter
    rootDir: skrepayshop-api
    buildCommand: npm run render:build
    startCommand: npm run render:start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: HOST
        value: "0.0.0.0"
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: COOKIE_SECRET
        generateValue: true
      - key: AUTH_MFA_ENCRYPTION_KEY
        generateValue: true
      - key: MEDUSA_BACKEND_URL
        value: https://api.skrepay.com
      - key: PLATFORM_URL
        value: https://skrepay.com
      - key: STORE_CORS
        value: https://skrepay.com,https://www.skrepay.com,https://app.skrepay.com
      - key: ADMIN_CORS
        value: https://skrepay.com,https://www.skrepay.com,https://app.skrepay.com,https://api.skrepay.com
      - key: AUTH_CORS
        value: https://skrepay.com,https://www.skrepay.com,https://app.skrepay.com,https://api.skrepay.com
      - key: STOREFRONT_URL
        value: https://skrepay.com
